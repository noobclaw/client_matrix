{
  "name": "noobclaw-desktop-native",
  "version": "0.1.0",
  "private": true,
  "description": "Native macOS desktop automation addon for NoobClaw sidecar. Replaces the osascript/screencapture/Python fallbacks with CGEvent + NSPasteboard + CGDisplayCreateImage for ~10x faster screenshots, smooth 60fps mouse motion, anti-bot-safe input injection, and reliable clipboard verification.",
  "main": "build/Release/noobclaw_desktop.node",
  "scripts": {
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean"
  },
  "devDependencies": {
    "node-addon-api": "^7.1.0",
    "node-gyp": "^11.0.0"
  }
}
